ToggleWidget launch -> switch
DatePickerWidget choose -> pick

GridTable event

widgetWriteEvent.gridTableID_currentRow_currentCol_widgetId -> repo widgetId : { widgetWriteId: { execute: (db, row, col) => {} }}

widget parameter write: 'off' -> nem keres a repóban íráskor.

Upload

gridtablecell -> cellSkin
gridtableheadercell -> cellHeaderSkin
gridtable -> hideIfNoData

dropbox -> backdrop, selectFirst

eventMap -> event.widgetid.(finished|started)

ImageWidget -> skin

Skins:

ImageWidget :
 warning_bpsp

TextWidget:
checkout_warning_bpsp
checkout_warning_message_bpsp

ButtonWidget:
blue_bg_bpsp
white_bg_bpsp
